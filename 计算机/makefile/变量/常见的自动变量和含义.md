# 自动变量
-  <font color=red>使用时前面加一个\$</font>  
-  "*"
    - 表示目标文件的名称，不包括目标文件的**扩展名**
- “ + ”
    - 表示所有的依赖文件，这些依赖文件之间以空格分开，按照出现的先后为顺序，其中可能包含重复的依赖文件
- “<”
    -  表示依赖项中第一个依赖文件的名称
-  “？”
    - 依赖项中，所有目标文件时间戳晚的依赖文件，依赖文件之间以空格分开
- "@"
    - 目标项中目标文件的名称
- “^”
    - 依赖项中，所有不重复的依赖文件，这些文件以空格隔开
